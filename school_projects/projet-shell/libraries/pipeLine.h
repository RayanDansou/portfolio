
char *** pipeCommands (char* buf); // renvoie chaque commande de la pipeLine sous forme de char** dans un seul tableau

void pipeRedi(char* buf);// pipes avec redirections


void pipeExec(char* buf); //execute la pipeLine